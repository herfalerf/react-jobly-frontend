import React from "react";

//Basic dumb component for when user tries to access a route that doesn't exist.
function NotFound() {
  return (
    <div>
      <h1>The page you're looking for doesn't exist</h1>
    </div>
  );
}

export default NotFound;
